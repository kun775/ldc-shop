import packageJson from "../../package.json"

export const APP_VERSION = packageJson.version
export const SOURCE_REPO = "kun775/ldc-shop"
export const SOURCE_REPO_URL = `https://github.com/${SOURCE_REPO}`
